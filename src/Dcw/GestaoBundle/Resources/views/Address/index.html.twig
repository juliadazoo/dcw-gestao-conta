{% extends 'DcwGestaoBundle::_layout.html.twig' %}

{% block body -%}
    <h1>Address list</h1>

    <table id="entity-table" class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Address</th>
                <th>Complement</th>
                <th>Number</th>
                <th>Neighborhood</th>
                <th>City</th>
                <th>State</th>
                <th>Zipcode</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('address_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.address }}</td>
                <td>{{ entity.complement }}</td>
                <td>{{ entity.number }}</td>
                <td>{{ entity.neighborhood }}</td>
                <td>{{ entity.city }}</td>
                <td>{{ entity.state }}</td>
                <td>{{ entity.zipcode }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('address_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('address_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('address_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
